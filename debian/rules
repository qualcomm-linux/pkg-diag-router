#!/usr/bin/make -f

export DH_VERBOSE = 1

%:
	dh $@

override_dh_auto_install:
ifeq ($(DEB_HOST_ARCH),arm64)
	mkdir -p $(CURDIR)/debian/qcom-diag-router/
	cp -a qcom-diag-router/arm64/* $(CURDIR)/debian/qcom-diag-router/
endif
	:

# binaries are already stripped
override_dh_dwz: ;
